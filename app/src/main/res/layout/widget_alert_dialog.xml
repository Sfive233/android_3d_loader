<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/darkBackground">

    <ImageView
        android:id="@+id/alert_icon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:padding="6dp"
        android:layout_margin="10dp"
        android:src="@drawable/ic_alert"
        android:background="@drawable/shape_triangle"
        android:backgroundTint="#FF9800"/>

    <TextView
        android:id="@+id/alert_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/alert_icon"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:padding="6dp"
        android:text="Make sure you are doing the right thing"
        android:textColor="@color/propertyText" />

    <Button
        android:id="@+id/alert_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/alert_detail"
        app:layout_constraintLeft_toLeftOf="parent"
        android:text="OK"
        android:textColor="@color/propertyText" />

    <Button
        android:id="@+id/alert_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/alert_detail"
        app:layout_constraintLeft_toRightOf="@id/alert_ok"
        app:layout_constraintRight_toRightOf="parent"
        android:text="No"
        android:textColor="@color/propertyText" />


</androidx.constraintlayout.widget.ConstraintLayout>